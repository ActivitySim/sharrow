name: sh-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.9
  - pip
  # required for testing
  - dask
  - filelock
  - ruff
  - jupyter
  - nbmake
  - networkx
  - notebook
  - numba>=0.57
  - numexpr
  - numpy>=1.19,<2
  - openmatrix
  - pandas>=1.2
  - pyarrow
  - pytest
  - pytest-cov
  - pytest-regressions
  - pytest-xdist
  - sparse
  - xarray
  - zarr

  - pip:
    - larch>=6
    - -e ..
