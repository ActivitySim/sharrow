name: sh-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.9
  - pip
  # required for testing
  - dask
  - filelock
  - ruff
  - jupyter
  - larch>=5.7.1
  - nbmake
  - networkx
  - notebook
  - numba>=0.54
  - numexpr
  - numpy>=1.19
  - openmatrix
  - pandas>=1.2
  - pyarrow
  - pytest
  - pytest-cov
  - pytest-regressions
  - pytest-xdist
  - sparse
  - xarray
  - zarr

  - pip:
    - -e ..
