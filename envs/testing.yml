name: testing-env
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pip
  - numpy>=1.19,<2
  - pandas>=1.2
  - pyarrow
  - xarray
  - dask
  - networkx
  - numba>=0.57
  - numexpr
  - sparse
  - filelock
  - ruff
  # required for testing
  - pytest
  - pytest-cov
  - pytest-regressions
  - pytest-xdist
  - nbmake
  - h5py
  - python-blosc2
  - hdf5plugin
  - zarr
  - pip:
    - larch>=6
